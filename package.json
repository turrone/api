{
  "name": "@turrone/api",
  "version": "0.1.0",
  "description": "REST API endpoints for powering Turrone Server",
  "main": "index.js",
  "scripts": {
    "coverage": "nyc report --reporter=json && codecov -f coverage/coverage-final.json",
    "doc": "npm run doc:api && npm run doc:dev",
    "doc:api": "apidoc -i src/api/ -o doc/api/",
    "doc:dev": "documentation build src/** -f html -o doc/dev/ --github",
    "test": "nyc mocha --exit"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "linters": {
      "*.js": [
        "eslint --fix",
        "git add"
      ],
      "*.{json,md,yml}": [
        "prettier --write",
        "git add"
      ]
    }
  },
  "apidoc": {
    "title": "Turrone Server REST API Endpoint Documentation",
    "sampleUrl": "http://localhost:8080"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/turrone/api.git"
  },
  "keywords": [
    "Turrone",
    "NuGet",
    "Chocolatey"
  ],
  "author": "",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/turrone/api/issues"
  },
  "homepage": "https://github.com/turrone/api#readme",
  "devDependencies": {
    "apidoc": "^0.17.7",
    "chai": "^4.2.0",
    "chai-http": "^4.3.0",
    "codecov": "^3.5.0",
    "documentation": "^11.0.0",
    "eslint": "^5.16.0",
    "eslint-config-prettier": "^4.3.0",
    "eslint-plugin-prettier": "^3.1.0",
    "husky": "^2.3.0",
    "lint-staged": "^8.1.7",
    "mocha": "^6.1.4",
    "nyc": "^14.1.1",
    "prettier": "^1.17.1"
  },
  "dependencies": {
    "express": "^4.17.0"
  }
}
